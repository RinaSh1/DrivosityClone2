//
//  OrderModel.swift
//  Homework1
//
//  Created by Drivosity Tetovo on 12.10.22.
//

/*import UIKit
 
 class OrderViewModel {
 
 var currentOrder: Order
 var currentIndex: Int = 0
 
 init(order: Order, index: Int){
 currentOrder = order
 currentIndex = index
 }
 
 var orderNumber: String {
 return "Order" + " " + currentOrder.orderRank.codingKey.stringValue
 }
 var address: String {
 return currentOrder.displayAddress.trimmingCharacters(in: .whitespacesAndNewlines)
 }
 var total: String {
 var total = "N/A*"
 if let ticket = currentOrder.ticketTotal {
 if ticket.customerTotal > 0 {
 total = "$\(ticket.customerTotal.stringValue(decimals: 2))"
 }
 }
 return total
 }
 }
 */
